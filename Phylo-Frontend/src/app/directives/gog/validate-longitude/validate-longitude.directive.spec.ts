import { ValidateLongitudeDirective } from './validate-longitude.directive';

describe('ValidateLongitudeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateLongitudeDirective();
    expect(directive).toBeTruthy();
  });
});
