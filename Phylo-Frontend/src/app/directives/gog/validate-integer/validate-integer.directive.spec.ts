import { ValidateIntegerDirective } from './validate-integer.directive';

describe('ValidateIntegerDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateIntegerDirective();
    expect(directive).toBeTruthy();
  });
});
