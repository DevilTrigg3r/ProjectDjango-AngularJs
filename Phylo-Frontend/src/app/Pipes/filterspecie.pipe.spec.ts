import { FilterspeciePipe } from './filterspecie.pipe';

describe('FilterspeciePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterspeciePipe();
    expect(pipe).toBeTruthy();
  });
});
