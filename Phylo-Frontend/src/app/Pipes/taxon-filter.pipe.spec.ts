import { TaxonFilterPipe } from './taxon-filter.pipe';

describe('TaxonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaxonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
