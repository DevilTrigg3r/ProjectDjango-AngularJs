import { FilterTaxonPipe } from './filter-taxon.pipe';

describe('FilterTaxonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTaxonPipe();
    expect(pipe).toBeTruthy();
  });
});
