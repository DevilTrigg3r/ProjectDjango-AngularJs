import { ValidateTimeDirective } from './validate-time.directive';

describe('ValidateTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateTimeDirective();
    expect(directive).toBeTruthy();
  });
});
