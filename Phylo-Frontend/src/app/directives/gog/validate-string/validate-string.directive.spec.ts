import { ValidateStringDirective } from './validate-string.directive';

describe('ValidateStringDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateStringDirective();
    expect(directive).toBeTruthy();
  });
});
