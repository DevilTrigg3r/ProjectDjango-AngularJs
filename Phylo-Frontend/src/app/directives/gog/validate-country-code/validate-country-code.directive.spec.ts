import { ValidateCountryCodeDirective } from './validate-country-code.directive';

describe('ValidateCountryCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCountryCodeDirective();
    expect(directive).toBeTruthy();
  });
});
