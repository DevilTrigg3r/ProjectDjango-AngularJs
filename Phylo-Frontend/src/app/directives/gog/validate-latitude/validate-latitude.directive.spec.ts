import { ValidateLatitudeDirective } from './validate-latitude.directive';

describe('ValidateLatitudeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateLatitudeDirective();
    expect(directive).toBeTruthy();
  });
});
