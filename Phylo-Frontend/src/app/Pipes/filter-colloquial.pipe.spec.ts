import { FilterColloquialPipe } from './filter-colloquial.pipe';

describe('FilterColloquialPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColloquialPipe();
    expect(pipe).toBeTruthy();
  });
});
